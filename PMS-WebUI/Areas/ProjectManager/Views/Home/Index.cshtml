@model List<ProjectDto>
<div id="loading">
    <div class="spinner-border text-primary" role="status">
        <span class="sr-only">Loading...</span>
    </div>
</div>

<div class="container mt-5">
        <div class="row">
            <div class="col-md-12">
                <h2 class="text-center mb-4">Projelerim</h2>
            </div>
        </div>
        <div class="row">
        @{
            if (Model.Count() == 0)
            {
                <div class="row">
                    <div class="col-md-12">
                        <h2 class="text-center mb-4">Proje Yok</h2>
                    </div>
                </div>
            }
            else
            {
                int counter = 0;
                foreach(var project in Model)
                {
                    if (counter % 3 == 0 && counter != 0)
                {
                    @:</div><div class="row">
                }
                    <div class="col-md-4">
                        <div class="card mb-4">
                            <div class="card-body">
                                <h5 class="card-title">@project.ProjectName</h5>
                                <p class="card-text"><strong>Öncelik: </strong>@project.Priority</p>
                                <p class="card-text"><strong>Başlangıç Tarihi: </strong>@project.StartDate</p>
                                <p class="card-text"><strong>Bitiş Tarihi: </strong>@project.EndDate</p>
                            </div>
                            <div class="card-footer">
                                <a asp-area="ProjectManager" asp-controller="Project" asp-action="Detail" asp-route-projectId="@project.Id" class="btn btn-primary">Projeyi Görüntüle</a>
                            </div>
                        </div>
                    </div>
                }
                if (counter % 3 == 0)
                    {
                         @:</div><div class="row">
                    }
                <div class="col-md-4">
                        <div class="card mb-4">
                            <div class="card-body">
                                <h5 class="card-title text-center">Yeni Proje Oluştur</h5>
                            </div>
                            <div class="card-footer text-center">
                                <a asp-area="ProjectManager" asp-controller="Project" asp-action="Add" asp-route-projectManagerId="@Model.FirstOrDefault().ProjectManagerId" class="btn btn-success">Proje Ekle</a>
                            </div>
                        </div>
                    </div>
            }
            }
        </div>
    </div>

    <div class="deleted">
      <a asp-area="ProjectManager" asp-controller="Project" asp-action="DeletedProjects" class="btn btn-danger btn-deleted">Durdurulan Projelerim</a>
    </div>
