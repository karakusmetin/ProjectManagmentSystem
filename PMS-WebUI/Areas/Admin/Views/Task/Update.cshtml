@model TaskUpdateDto


<div class="admin-container">
    <div class="Task-add">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Proje Görev Yönetimi /</span> Görev Ekle</h4>

        <div class="row">
            <div class="col-md-12">
                <div class="card mb-4">
                    <form asp-controller="Task" asp-action="Update" method="post" enctype="multipart/form-data">
                        <div class="card-body">
                            <input type="hidden" asp-for="Id" />
                            <div>
                                <label asp-for="TaskName" class="form-label">Proje Adı</label>
                                <input asp-for="TaskName" type="text" class="form-control mb-2" placeholder="Görev Adı..." aria-describedby="defaultFormControlHelp">
                                <span asp-validation-for="TaskName" class="text-danger"></span>
                            </div>
                            <div>
                                <label asp-for="Description" for="defaultFormControlInput" class="form-label">Proje Açıklaması</label>
                                <textarea asp-for="Description" type="text" class="form-control mb-2" style="min-height:100px" placeholder="Görev Detayı..." aria-describedby="defaultFormControlHelp"></textarea>
                                <span asp-validation-for="Description" class="text-danger"></span>
                            </div>
                            <div>
                                <label asp-for="StartDate" for="defaultFormControlInput" class="form-label">Görev Başlangıç Tarihi</label>
                                <input asp-for="StartDate" class="form-control mb-2" type="datetime-local" id="html5-datetime-local-input">
                                <span asp-validation-for="StartDate" class="text-danger"></span>
                            </div>

                            <div>
                                <label asp-for="EndDate" for="defaultFormControlInput" class="form-label">Görev Bitiş Tarihi</label>
                                <input asp-for="EndDate" class="form-control mb-2" type="datetime-local" id="html5-datetime-local-input">
                                <span asp-validation-for="EndDate" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label asp-for="Priority" for="defaultSelect" class="form-label">Görev Önceliği</label>
                                <select asp-for="Priority" asp-items="Html.GetEnumSelectList<PriorityLevel>()" id="defaultSelect" class="form-select">
                                </select>
                            </div>

                            <div class="form group">
                                <button type="submit" class="btn btn-primary">Kaydet</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .form-label {
        font-weight: bold; /* Kalın font */
        color: #333; /* Metin rengi */
    }

    /* Select kutusu */
    .form-select {
        width: 100%; /* Genişlik tam olarak doldurulsun */
        padding: 0.375rem 0.75rem; /* Dolgu */
        line-height: 1.5; /* Satır yüksekliği */
        color: #495057; /* Metin rengi */
        background-color: #fff; /* Arka plan rengi */
        border: 1px solid #3e2481; /* Kenarlık */
        border-radius: 0.25rem; /* Kenar yuvarlaklığı */
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out; /* Geçiş efekti */
    }

        /* Seçenekler */
        .form-select option {
            color: #495057; /* Metin rengi */
        }

</style>